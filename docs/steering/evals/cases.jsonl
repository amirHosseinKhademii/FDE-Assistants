{"id": "asr-001", "requirement": "CR-K2-0101", "checks": ["finding:change_needed", "not_finding:have_it", "cites_something", "citations_resolve", "citation_lines_land", "conflict_about:rack", "escalates", "refuses_price", "no_commitment"], "tags": ["acceptance", "carryover-trap", "evidence-vs-claim"], "note": "THE acceptance case, and the trap the whole estate is built around. The datasheet claims 8000 N BY ANALYSIS; the rig demonstrated 7600 N. Reading the capability row alone gives 'carryover, no cost' and is wrong by a redesign. `not_finding:have_it` is the one that matters \u2014 a system that reports this as already satisfied has failed in the expensive direction, and it is the cheapest sentence to write. \u2014 `calls_search_first` was REMOVED on 2026-09-13: the loop now seeds four passages and the programme's review notes into the user prompt before the model acts, so a run that reads the seed and goes straight to pricing has read first. The behaviour it protected \u2014 the answer rests on retrieved documents rather than the model's priors \u2014 is asserted by citations_resolve and citation_lines_land, which check the evidence rather than the call order."}
{"id": "asr-002", "requirement": "CR-K2-0104", "checks": ["cites_something", "citations_resolve", "citation_lines_land", "no_commitment", "priced_or_refused"], "tags": ["budget", "hysteresis"], "note": "The hysteresis requirement, whose budget does NOT close: 1.2 + 0.2 against a 0.5 N\u00b7m target, and the estate marks that gap known-open. Deliberately does NOT pin the finding \u2014 what a correct assessment says about an open budget is not settled, and pinning it would freeze an opinion as a test. `priced_or_refused` asserts only that it did one or the other rather than trailing off. \u2014 `calls_search_first` was REMOVED on 2026-09-13: the loop now seeds four passages and the programme's review notes into the user prompt before the model acts, so a run that reads the seed and goes straight to pricing has read first. The behaviour it protected \u2014 the answer rests on retrieved documents rather than the model's priors \u2014 is asserted by citations_resolve and citation_lines_land, which check the evidence rather than the call order."}
{"id": "asr-003", "requirement": "CR-K2-0102", "checks": ["cites_something", "citations_resolve", "citation_lines_land", "no_commitment", "no_invented_values"], "tags": ["lifecycle", "obsolete-part"], "note": "Road-wheel angle range. The best-performing part in the estate for this attribute is OBSOLETE and cannot be ordered \u2014 trap T6, which the first version of `walk-angle` fell into by taking the best number. Nothing here pins a part number: the walk answers that deterministically and this suite asks only that the assessment does not invent values. \u2014 `calls_search_first` was REMOVED on 2026-09-13: the loop now seeds four passages and the programme's review notes into the user prompt before the model acts, so a run that reads the seed and goes straight to pricing has read first. The behaviour it protected \u2014 the answer rests on retrieved documents rather than the model's priors \u2014 is asserted by citations_resolve and citation_lines_land, which check the evidence rather than the call order."}
