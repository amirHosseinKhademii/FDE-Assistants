/**
 * ONE REAL ASSESSMENT, CAPTURED. Generated — do not edit by hand.
 *
 * NOTHING HAS BEEN CAPTURED YET, and `null` is the honest value for that. The
 * page renders no example rather than a plausible one; a made-up specimen on a
 * page whose whole argument is "here is the real output" would be the worst
 * thing on this site.
 *
 * Run `pnpm steering:worked-example` to fill it in. That spends money — a
 * multi-turn loop, tens of seconds, a few cents — which is why it is a command
 * somebody chooses to run rather than a build step.
 */
import type { WorkedExample } from './worked-example';

export const WORKED: WorkedExample | null = null;
