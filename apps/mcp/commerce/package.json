{
  "name": "@thornbury/commerce-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Thornbury Goods — the MCP server. See docs/commerce/MCP-STEPS.md.",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "serve": "ts-node src/server.ts",
    "handshake": "ts-node src/cli/handshake.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@types/node": "^22.7.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
